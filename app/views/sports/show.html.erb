<div class="row-fluid"><div class="span3"><h1><%=	@sport.name %></h1></div></div>

<% if @sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sport.errors.count, "error") %> prohibited this sport from being saved:</h2>

      <ul>
      <% @sport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--</div><br>-->

  <div class="row-fluid"><div class="span1"><h6>Sports</h6></div></div>
  <ul>
    <%	for t in @sport.leagues  %>
        <div class="row-fluid"><div class="span2"><li><%= link_to t.name, t %></li></div></div>
    <% end %>
  </ul>

</div>

<%= link_to 'Edit', edit_sport_path(@sport) %> |
<%= link_to 'Back', sports_path %>
